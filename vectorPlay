v <- c(17,5,7,30,8,2,6,24,40,19)
v1 <- as.double(v)

cat(v1,"\n")

v2 <- sort(v1)

cat(v2, "\n")

m <- min(v2)
M <- max(v2)

cat("The min is:", m ,"\n")
cat("The max is:", M, "\n")

a <- mean(v2)

cat("The average is:", a)
